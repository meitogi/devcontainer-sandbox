{
  "name": "@meitogi/devcontainer-sandbox",
  "version": "1.5.0",
  "private": true,
  "description": "Firewalled devcontainer base image, Claude Code preinstalled — Node 24, hooks dispatcher. Published to GHCR, never to npm; this manifest exists to carry the base version (read by the publish workflow, baked as LABEL org.stitchu.base.version).",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meitogi/devcontainer-sandbox.git"
  },
  "license": "MIT"
}
